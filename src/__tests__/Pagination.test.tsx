import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';
import UserData from '../components/dashboard_components/UserData'


test('Should render the correct number of users in one page', () => {
  //render(<UserData />);
  
})