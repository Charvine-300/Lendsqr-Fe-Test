export interface IFormInput {
    email: string;
    password: string;
}

export interface MenuProps {
    id: number;
    icon: string;
    name: string;
}

export interface UsersProps {
    id: number;
    feature: string;
    icon: string;
    total: string;
    bg: string;
}